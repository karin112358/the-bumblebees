// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: replace with the real domain once it exists. Used for canonical URLs and sitemaps.
  site: 'https://TODO-replace-with-real-domain.example',
});
